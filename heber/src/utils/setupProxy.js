// const { createProxyMiddleware } = require('http-proxy-middleware');

// module.exports = function (app) {
//   app.use(
//     '/structure', // Specify the path you want to proxy requests for
//     createProxyMiddleware({
//       target: 'http://localhost:3000/', // Specify the target URL
//       changeOrigin: true, // Changes the origin of the host header to the target URL
//       secure: false, // Disable SSL verification (optional, for development only)
//       // Additional options can be configured here as needed
//     })
//   );
// };
